# Copyright 2023 Nokia
# Licensed under the BSD 3-Clause License.
# SPDX-License-Identifier: BSD-3-Clause

- hosts: clab
  gather_facts: no
  tasks:
    # this task is expected to fail as the path is not valid
    - name: json RPC set wrong path
      nokia.srl.jsonrpc_set:
        commands:
          - action: update
            path: /system/foo
            value:
              location: SomeLocation
              contact: SomeContact
      register: set_return

    - debug:
        var: set_return
